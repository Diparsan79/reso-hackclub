[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "resolution_week1_flameX"
version = "0.0.1"
dependencies = []

[project.scripts]
todo = "resolution_week1_flameX.main:main"
[tool.hatch.build.targets.wheel]
packages = ["src/resolution_week1_flamex"]